// src/index.ts
import { defineComponent, h } from "vue";
var Html = defineComponent({
  name: "Html",
  props: {
    lang: {
      type: String,
      default: "en"
    },
    dir: {
      type: String,
      default: "ltr"
    }
  },
  setup(props, { slots }) {
    return () => {
      var _a;
      return h(
        "html",
        {
          lang: props.lang,
          dir: props.dir
        },
        (_a = slots.default) == null ? void 0 : _a.call(slots)
      );
    };
  }
});
export {
  Html
};
