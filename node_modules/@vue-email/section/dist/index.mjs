// src/index.ts
import { defineComponent, h } from "vue";
var Section = defineComponent({
  name: "Section",
  setup(_, { slots }) {
    return () => {
      var _a;
      return h(
        "table",
        {
          "align": "center",
          "width": "100%",
          "border": "0",
          "cellPadding": "0",
          "cellSpacing": "0",
          "role": "presentation"
        },
        [h("tbody", [h("tr", [h("td", (_a = slots.default) == null ? void 0 : _a.call(slots))])])]
      );
    };
  }
});
export {
  Section
};
