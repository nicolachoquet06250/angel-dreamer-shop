// src/index.ts
import { defineComponent, h } from "vue";
var Text = defineComponent({
  name: "Text",
  setup(_, { slots }) {
    return () => {
      var _a;
      return h(
        "p",
        {
          "style": {
            fontSize: "14px",
            lineHeight: "24px",
            margin: "16px 0"
          }
        },
        (_a = slots.default) == null ? void 0 : _a.call(slots)
      );
    };
  }
});
export {
  Text
};
