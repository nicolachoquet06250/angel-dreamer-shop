// src/index.ts
import { defineComponent, h } from "vue";
var Body = defineComponent({
  name: "Body",
  setup(_, { slots }) {
    return () => {
      var _a;
      const bodyNode = h("body", (_a = slots.default) == null ? void 0 : _a.call(slots));
      return bodyNode;
    };
  }
});
export {
  Body
};
